v1.19
更新说明：增加无视频报警机制。通过组播发送到监控终端。
v1.18
更新说明：增加刘星的cpu监测程序。修改了trap给网管的频率和机制。
v1.16
更新说明：修改了默认量化系数和帧率，并且修改了通道的带宽。
v1.14
更新说明：增加了摄像机无信号时，向网管上报的功能。
v1.11
更新说明：修改了一块卡不能编辑4路d1格式的bug。
v1.0
============
程序功能描述
============
1.编码服务器程序。能够接收64路图象数据，编码以后再通过64路发送到网络。

2.监控终端的组播IP和端口由网管获取，否则无法运行。

3.可以动态查询和设置图象的参数，例如亮度，对比度，饱和度和色调等等。

4.可以设置字幕迭加功能。

=====
查询：
=====
1、VideoPara 亮度，对比度，饱和度和色调
	通道：
		格式：encoder:qp
	站：	格式：encoder:[通道号] qp
		成功：encoder:ret:get video para failed
		失败：encoder:ret:channel [录像id]-brightness [数值]-contrast [数值]-saturation [数值]-hue [数值]
2、IBPMode 关键针，B针，针率
	通道：
		格式：encoder:qi
	站：
		格式：encoder:[通道号] qi
		成功：encoder:ret:get video para failed
		失败：encoder:ret:channel [录像id]-kframe [数值]-bframe [数值]-framerate [数值]
3、Quant 量化系数
	通道：
		格式：encoder:qq
	站：	
		格式：encoder:[通道号] qq
		成功：encoder:ret:get video para failed
		失败：encoder:ret:channel [录像id]-iquant [数值]-bquant [数值]-pquant [数值]
4、视频信号
	通道：
		格式：encoder:qx
	站：
		格式：encoder:[通道号] qx
		成功：encoder:ret:normal
		失败：encoder:ret:error
	开始报警：  
		格式：encoder:video_disappear [通道号] [时间]
	取消报警：
		格式：encoder:cancel:video_disappear [通道号] [时间]  
	应答：  
		格式：encoder:[通道号] ack_video_disappear
=====
设置：
=====
1、VideoPara 亮度，对比度，饱和度和色调
	通道：
		格式：encoder:cp [亮度参数] [对比度参数] [饱和度参数] [色调参数]
		   [0-255]     [0-255]	     [0-255]       [0-255]
	站：
		格式：encoder:[通道号] cp [亮度参数] [对比度参数] [饱和度参数] [色调参数]
		成功：返回查询信息	
		失败：encoder:ret:set video para failed
2、IBPMode 关键针，B针，针率
	通道：
		格式：encoder:ci [关键针参数] [B针参数] [针率参数]
		   	 [12 < x]   [0,1或2] [1-30]
	站：	格式：encoder:[通道号] ci [关键针参数] [B针参数] [针率参数]
		成功：返回查询信息
		失败：encoder:ret:set ibpmode failed
3、Quant 量化系数
	通道：
		格式：encoder:cq [I参数] [P参数] [B参数]
		  	  [12-30]  [12-30]  [12-30]
	站：	格式：encoder:[通道号] cq [I参数] [P参数] [B参数]
		成功：返回查询信息
		失败：encoder:ret:set quant failed
=============
设置字幕迭加：
=============
SetOsd
	通道：
		格式：encoder:se [0/1]表示使能
	站：	格式：encoder:[通道号] se [0/1]表示使能
		成功：启用：ret:set osd on
	      	停止：encoder:ret:set osd off
		失败：encoder:ret:set osd failed
OsdDisplayMode
	通道：
		格式：encoder:os [行号] [起始位置X坐标] [起始位置Y坐标] [半透明度] [自定义字符]
		    	             0/1	[8的倍数]        [0-287]          [0/1]     [64个字符]
	站：	格式：encoder:[通道号] os [行号] [起始位置X坐标] [起始位置Y坐标] [半透明度] [自定义字符]
		成功：encoder:ret:set osd mode ok

	64个字符定义：由<>括起来的0-10之间的数字，如<0>, <2>表示日期时间，其他为一般字符。

============
程序变量定义
============
1.关于监控终端
MULTICASTIP 		与监控终端通信的组播IP，由网管获取。

MULTICASTPORT	与监控终端通信的端口号，由网管获取。

2.关于图象参数
videoParam		保存亮度，对比度，饱和度和色调等参数的数组。

IBPMode		保存关键针，B针和针率参数的数组。

Quant			保存量化系数参数的数组。
	
