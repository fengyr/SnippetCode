[本机ip地址配置]-ip.conf
conf = {'LOCAL':[ip_address, netmask, gateway], ...}

[摄像机列表配置]-video_list.conf
conf = [[section, [camera_name, server_ip, server_port, source, author]], ...]

[用户登录配置]-users.conf
conf = {name:[password, author], ...}
